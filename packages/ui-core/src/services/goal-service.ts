import type { StoredGoal } from "../types/index.js";

// Stub — real implementation lives in packages/cli/src/services/goal-service.ts
export async function getGoals(): Promise<StoredGoal[]> {
  return [];
}
