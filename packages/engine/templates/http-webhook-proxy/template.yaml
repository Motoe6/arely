version: "1.0.0"
name: "HTTP Webhook Proxy - {{ param:target_url }}"
trigger:
  type: webhook
steps:
  - id: proxy
    type: http
    input:
      url: "{{ param:target_url }}"
      method: "{{ param:method }}"
      forwardHeaders: "{{ param:forward_headers }}"
